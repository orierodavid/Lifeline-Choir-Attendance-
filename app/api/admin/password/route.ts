import {NextResponse} from 'next/server'
const URL=process.env.NEXT_PUBLIC_SUPABASE_URL||'https://lkxvcphlrvaheccjraxm.supabase.co';const KEY=process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY||process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY||''
function token(req:Request){return req.headers.get('cookie')?.match(/(?:^|; )lifeline_admin_token=([^;]+)/)?.[1]}
async function admin(req:Request){const t=token(req);if(!t)return null;const r=await fetch(`${URL}/auth/v1/user`,{headers:{apikey:KEY,Authorization:`Bearer ${t}`},cache:'no-store'});if(!r.ok)return null;const u=await r.json();return String(u?.app_metadata?.role||'')==='admin'||String(u?.email||'').toLowerCase()===(process.env.ADMIN_EMAIL||'orierodavid@gmail.com').toLowerCase()?{t,u}:null}
export async function PATCH(req:Request){try{const a=await admin(req);if(!a)return NextResponse.json({error:'Unauthorized'},{status:401});const {password,confirm}=await req.json();if(typeof password!=='string'||password.length<8||password!==confirm)return NextResponse.json({error:'Password must be at least 8 characters and match confirmation.'},{status:400});const r=await fetch(`${URL}/auth/v1/user`,{method:'PUT',headers:{apikey:KEY,Authorization:`Bearer ${a.t}`,'Content-Type':'application/json'},body:JSON.stringify({password}),cache:'no-store'});if(!r.ok)return NextResponse.json({error:'Unable to update password.'},{status:500});return NextResponse.json({ok:true})}catch{return NextResponse.json({error:'Unable to update password.'},{status:500})}}
