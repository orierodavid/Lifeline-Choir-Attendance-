import {NextResponse} from 'next/server'
const URL=process.env.NEXT_PUBLIC_SUPABASE_URL||'https://lkxvcphlrvaheccjraxm.supabase.co';const KEY=process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY||process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY||''
export async function GET(){try{const r=await fetch(`${URL}/rest/v1/rpc/get_attendance_location`,{headers:{apikey:KEY,Authorization:`Bearer ${KEY}`},cache:'no-store'});const rows=await r.json();if(!r.ok||!rows?.[0])return NextResponse.json({error:'Unable to load attendance location.'},{status:500});const s=rows[0];return NextResponse.json({church_name:s.church_name,church_latitude:s.church_latitude,church_longitude:s.church_longitude,church_radius_meters:Number(s.church_radius_meters)||300},{headers:{'Cache-Control':'no-store, no-cache, must-revalidate, max-age=0'}})}catch{return NextResponse.json({error:'Unable to load attendance location.'},{status:500})}}