import { NextResponse } from 'next/server'
const URL=process.env.NEXT_PUBLIC_SUPABASE_URL||'https://lkxvcphlrvaheccjraxm.supabase.co';const KEY=process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY||process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY||''
function distance(a:number,b:number,c:number,d:number){const r=6371000,p=Math.PI/180,x=(c-a)*p,y=(d-b)*p,q=Math.sin(x/2)**2+Math.cos(a*p)*Math.cos(c*p)*Math.sin(y/2)**2;return 2*r*Math.asin(Math.sqrt(q))}
function lagosDate(){return new Intl.DateTimeFormat('en-CA',{timeZone:'Africa/Lagos',year:'numeric',month:'2-digit',day:'2-digit'}).format(new Date())}
export async function POST(req:Request){try{const{name,pin,lat,lng,accuracy}=await req.json();const cleanName=typeof name==='string'?name.trim().replace(/\s+/g,' '):'';if(!cleanName||typeof pin!=='string'||!/^\d{4}$/.test(pin))return NextResponse.json({error:'Enter your first or last name and 4-digit PIN.'},{status:400});if(typeof lat!=='number'||typeof lng!=='number'||!Number.isFinite(lat)||!Number.isFinite(lng))return NextResponse.json({error:'A valid location is required.'},{status:400});const sr=await fetch(`${URL}/rest/v1/rpc/get_attendance_location`,{headers:{apikey:KEY,Authorization:`Bearer ${KEY}`},cache:'no-store'});const rows=await sr.json();if(!sr.ok||!rows?.[0])return NextResponse.json({error:'Unable to read the current attendance location.'},{status:500});const s=rows[0],churchLat=Number(s.church_latitude),churchLng=Number(s.church_longitude),radius=Number(s.church_radius_meters)||300;if(!Number.isFinite(churchLat)||!Number.isFinite(churchLng)||radius<=0)return NextResponse.json({error:'The super admin has not configured a valid attendance location yet.'},{status:500});const metres=distance(churchLat,churchLng,lat,lng);if(metres>radius)return NextResponse.json({error:`You are ${Math.round(metres)}m from ${String(s.church_name||'the selected attendance location')}. You must be within ${radius}m to check in.`},{status:403});const memberResponse=await fetch(`${URL}/rest/v1/rpc/check_choir_member`,{method:'POST',headers:{apikey:KEY,Authorization:`Bearer ${KEY}`,'Content-Type':'application/json'},body:JSON.stringify({p_name:cleanName,p_pin:pin}),cache:'no-store'});if(!memberResponse.ok)return NextResponse.json({error:'Attendance service is temporarily unavailable.'},{status:503});const members=await memberResponse.json(),member=Array.isArray(members)?members[0]:members;if(!member)return NextResponse.json({error:'Name or PIN is incorrect. Please try again.'},{status:401});const insert=await fetch(`${URL}/rest/v1/rpc/record_choir_attendance`,{method:'POST',headers:{apikey:KEY,Authorization:`Bearer ${KEY}`,'Content-Type':'application/json'},body:JSON.stringify({p_member_id:member.id,p_attendance_date:lagosDate(),p_lat:lat,p_lng:lng,p_accuracy:typeof accuracy==='number'?accuracy:null}),cache:'no-store'});if(!insert.ok)return NextResponse.json({error:'Unable to record attendance. Please try again.'},{status:503});const result=await insert.json();if(!result||!result.length)return NextResponse.json({error:'You have already checked in for today.'},{status:409});return NextResponse.json({ok:true,message:'Attendance recorded',member:{name:member.full_name},checkedInAt:new Date().toISOString()})}catch{return NextResponse.json({error:'Unable to process check-in.'},{status:500})}}