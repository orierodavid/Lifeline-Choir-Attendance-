'use client'
import {useState} from 'react'
const VOICE_PARTS=['Soprano','Alto','Tenor','Musician']
function validPin(p:string){if(!/^\d{4}$/.test(p)||new Set(p).size!==4)return false;const n=p.split('').map(Number);return !(n.every((v,i)=>i===0||v===n[i-1]+1)||n.every((v,i)=>i===0||v===n[i-1]-1))}
export default function Register(){const[first_name,setFirst]=useState(''),[last_name,setLast]=useState(''),[voice_part,setVoice]=useState(''),[pin,setPin]=useState(''),[confirm,setConfirm]=useState(''),[error,setError]=useState(''),[done,setDone]=useState(false),[loading,setLoading]=useState(false);async function submit(){setError('');if(!first_name.trim()||!last_name.trim()||!VOICE_PARTS.includes(voice_part))return setError('Complete your details to continue.');if(!validPin(pin))return setError('PIN must contain four different digits. Avoid simple sequences such as 1234.');if(pin!==confirm)return setError('PIN confirmation does not match.');setLoading(true);try{const r=await fetch('/api/register',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({first_name:first_name.trim(),last_name:last_name.trim(),voice_part,pin})});const d=await r.json();if(!r.ok)throw new Error(d.error||'Registration failed');setDone(true)}catch(e:any){setError(e.message||'Registration failed.')}finally{setLoading(false)}}return <main className="h-app"><section className="h-workspace" style={{marginLeft:0,width:'100%'}}><header className="h-topbar"><div className="h-brand"><div className="h-logo">CB</div><div><b>CALVARY BIBLE CHURCH</b><span>Lifeline Choir</span></div></div><div className="h-top-actions"><span className="h-live">● MEMBER ACCESS</span></div></header><div className="h-content" style={{maxWidth:980}}>{done?<div className="h-card" style={{maxWidth:680,margin:'60px auto',padding:'54px 42px',textAlign:'center'}}><div className="h-kpi-icon green" style={{margin:'0 auto 22px',width:58,height:58,fontSize:25}}>✓</div><div className="h-breadcrumb">LIFELINE CHOIR <span>/</span> REGISTRATION</div><h1 style={{font:'800 32px Manrope',letterSpacing:'-.06em',margin:'8px 0'}}>Welcome to Lifeline.</h1><p style={{fontSize:11,color:'#929aa7',margin:'10px 0 26px'}}>Your member profile is ready. Keep your four-digit PIN private.</p><a className="h-primary" href="/" style={{display:'inline-flex',textDecoration:'none'}}>CONTINUE TO CHECK-IN →</a></div>:<><div className="h-page-head" style={{marginBottom:18}}><div><div className="h-breadcrumb">LIFELINE CHOIR <span>/</span> MEMBER ACCESS</div><h1>Create your profile.</h1><p>One-time registration. Then checking in takes seconds.</p></div><div className="h-date-pill">SECURE REGISTRATION</div></div><div className="h-card" style={{maxWidth:760,margin:'0 auto',padding:26}}><div className="h-card-head"><div><h3>Member details</h3><span>Use the name and choir role you are known by.</span></div><span className="h-live">STEP 01</span></div><div style={{display:'grid',gridTemplateColumns:'1fr 1fr',gap:12}}><div className="field"><label className="label">FIRST NAME</label><input className="input" value={first_name} onChange={e=>setFirst(e.target.value)} placeholder="First name" autoComplete="given-name"/></div><div className="field"><label className="label">LAST NAME</label><input className="input" value={last_name} onChange={e=>setLast(e.target.value)} placeholder="Last name" autoComplete="family-name"/></div></div><div className="field" style={{marginTop:12}}><label className="label">CHOIR ROLE</label><select className="input" value={voice_part} onChange={e=>setVoice(e.target.value)}><option value="">Select role</option>{VOICE_PARTS.map(v=><option key={v}>{v}</option>)}</select></div><div className="h-security-banner" style={{marginTop:22}}><div style={{fontSize:18}}>02</div><div><b>Private PIN</b><span>Create a four-digit PIN with different numbers. It is never displayed in the admin dashboard.</span></div></div><div style={{display:'grid',gridTemplateColumns:'1fr 1fr',gap:12,marginTop:12}}><div className="field"><label className="label">CREATE PIN</label><input className="input pin" inputMode="numeric" maxLength={4} value={pin} onChange={e=>setPin(e.target.value.replace(/\D/g,'').slice(0,4))} placeholder="••••"/></div><div className="field"><label className="label">CONFIRM PIN</label><input className="input pin" inputMode="numeric" maxLength={4} value={confirm} onChange={e=>setConfirm(e.target.value.replace(/\D/g,'').slice(0,4))} placeholder="••••"/></div></div>{error&&<p className="form-error">{error}</p>}<button className="h-primary" style={{width:'100%',marginTop:18}} disabled={loading} onClick={submit}>{loading?'CREATING PROFILE…':'CREATE PROFILE →'}</button><div className="public-links" style={{marginTop:18}}><span>Already registered?</span><a className="link-text" href="/">Go to check-in →</a></div></div></>}</div></section></main>}
