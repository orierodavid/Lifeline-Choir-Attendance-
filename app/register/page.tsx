'use client'
import {useState} from 'react'
const VOICE_PARTS=['Soprano','Alto','Tenor','Musician']
function validPin(p:string){if(!/^\d{4}$/.test(p)||new Set(p).size!==4)return false;const n=p.split('').map(Number);return !(n.every((v,i)=>i===0||v===n[i-1]+1)||n.every((v,i)=>i===0||v===n[i-1]-1))}
export default function Register(){const[first_name,setFirst]=useState(''),[last_name,setLast]=useState(''),[voice_part,setVoice]=useState(''),[pin,setPin]=useState(''),[confirm,setConfirm]=useState(''),[error,setError]=useState(''),[done,setDone]=useState(false),[loading,setLoading]=useState(false);async function submit(){setError('');if(!first_name.trim()||!last_name.trim()||!VOICE_PARTS.includes(voice_part))return setError('Complete your name and select your role.');if(!validPin(pin))return setError('Choose four different digits. Simple sequences such as 1234 are not allowed.');if(pin!==confirm)return setError('Your PINs do not match.');setLoading(true);try{const r=await fetch('/api/register',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({first_name:first_name.trim(),last_name:last_name.trim(),voice_part,pin})});const d=await r.json();if(!r.ok)throw new Error(d.error||'Registration failed');setDone(true)}catch(e:any){setError(e.message||'Registration failed.')}finally{setLoading(false)}}if(done)return <main className="public-shell"><section className="public-card success"><div className="public-top"><div className="brand-lockup"><div className="church-mark">CB</div><div><b>CALVARY BIBLE CHURCH</b><span>Lifeline Choir</span></div></div><span className="status-chip">WELCOME</span></div><div className="success-icon">✓</div><span className="public-kicker">PROFILE CREATED</span><h1>Welcome to Lifeline.</h1><p>Your member profile is ready. Keep your PIN private — administrators cannot see it.</p><a className="primary public-cta" href="/">Continue to check-in →</a></section></main>;return <main className="public-shell"><section className="public-card register-card"><div className="public-top"><div className="brand-lockup"><div className="church-mark">CB</div><div><b>CALVARY BIBLE CHURCH</b><span>Lifeline Choir</span></div></div><span className="status-chip">REGISTRATION</span></div><div className="public-intro"><span>MEMBER REGISTRATION</span><h1>Join Lifeline.</h1><p>Create your member profile in under a minute. Your PIN belongs to you and is never shown to administrators.</p></div><div className="step-label"><span>01</span><b>YOUR DETAILS</b></div><div className="form-grid"><div className="field"><label className="label">First name</label><input className="input" value={first_name} onChange={e=>setFirst(e.target.value)} placeholder="First name" autoComplete="given-name"/></div><div className="field"><label className="label">Last name</label><input className="input" value={last_name} onChange={e=>setLast(e.target.value)} placeholder="Last name" autoComplete="family-name"/></div></div><div className="field"><label className="label">Choir role</label><select className="input" value={voice_part} onChange={e=>setVoice(e.target.value)}><option value="">Select your role</option>{VOICE_PARTS.map(v=><option key={v}>{v}</option>)}</select></div><div className="step-label pin-step"><span>02</span><b>PRIVATE PIN</b></div><div className="pin-grid"><div className="field"><label className="label">Create PIN</label><input className="input pin" inputMode="numeric" maxLength={4} value={pin} onChange={e=>setPin(e.target.value.replace(/\D/g,'').slice(0,4))} placeholder="••••"/></div><div className="field"><label className="label">Confirm PIN</label><input className="input pin" inputMode="numeric" maxLength={4} value={confirm} onChange={e=>setConfirm(e.target.value.replace(/\D/g,'').slice(0,4))} placeholder="••••"/></div></div><div className="security-note"><div className="security-lock">⌕</div><div><strong>Your PIN is private</strong><span>Use four different digits. Avoid simple sequences such as 1234. Admins cannot view your PIN.</span></div></div>{error&&<p className="form-error" role="alert">{error}</p>}<button className="primary" disabled={loading} onClick={submit}>{loading?'CREATING PROFILE…':'CREATE MY PROFILE'}</button><div className="public-links"><span>Already registered?</span><a className="link-text" href="/">Go to attendance →</a></div></section></main>}