'use client'
import {useState} from 'react'
const VOICE_PARTS=['Soprano','Alto','Tenor']
function validPin(p:string){if(!/^\d{4}$/.test(p))return false;if(new Set(p).size!==4)return false;const n=p.split('').map(Number);if(n.every((v,i)=>i===0||v===n[i-1]+1)||n.every((v,i)=>i===0||v===n[i-1]-1))return false;return true}
export default function Register(){const[first_name,setFirst]=useState(''),[last_name,setLast]=useState(''),[voice_part,setVoice]=useState(''),[pin,setPin]=useState(''),[confirm,setConfirm]=useState(''),[error,setError]=useState(''),[done,setDone]=useState(false),[loading,setLoading]=useState(false)
async function submit(){setError('');if(!first_name.trim()||!last_name.trim()||!VOICE_PARTS.includes(voice_part))return setError('Please complete your name and select your voice part.');if(!validPin(pin))return setError('PIN must contain four different digits and cannot be a simple sequence.');if(pin!==confirm)return setError('PINs do not match.');setLoading(true);try{const r=await fetch('/api/register',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({first_name,last_name,voice_part,pin})});const d=await r.json();if(!r.ok)throw new Error(d.error||'Registration failed');setDone(true)}catch(e:any){setError(e.message)}finally{setLoading(false)}}
if(done)return <main className="shell"><section className="card success"><div className="logo-mark">✦</div><div className="brand">CALVARY BIBLE CHURCH</div><div className="system-name">Lifeline Choir Attendance System</div><div className="check">✓</div><h1 className="title">Registration complete</h1><p className="sub">Your choir details have been registered. Keep your PIN private and use it for attendance check-in.</p><a className="link" href="/">Go to check-in</a></section></main>
return <main className="shell"><section className="card"><div className="logo-mark">✦</div><div className="brand">CALVARY BIBLE CHURCH</div><div className="system-name">Lifeline Choir Attendance System</div><div className="rule"/><h1 className="title">Join the Choir</h1><p className="sub">Register your details once. You will use your name and PIN to check attendance.</p><div className="field"><label className="label">First name</label><input className="input" value={first_name} onChange={e=>setFirst(e.target.value)} placeholder="First name"/></div><div className="field"><label className="label">Last name</label><input className="input" value={last_name} onChange={e=>setLast(e.target.value)} placeholder="Last name"/></div><div className="field"><label className="label">Voice part</label><select className="input" value={voice_part} onChange={e=>setVoice(e.target.value)}><option value="">Select voice part</option>{VOICE_PARTS.map(v=><option key={v}>{v}</option>)}</select></div><div className="field"><label className="label">Choose 4-digit PIN</label><input className="input pin" inputMode="numeric" maxLength={4} value={pin} onChange={e=>setPin(e.target.value.replace(/\D/g,'').slice(0,4))} placeholder="Choose PIN"/></div><div className="field"><label className="label">Confirm PIN</label><input className="input pin" inputMode="numeric" maxLength={4} value={confirm} onChange={e=>setConfirm(e.target.value.replace(/\D/g,'').slice(0,4))} placeholder="Confirm PIN"/></div>{error&&<p className="muted" role="alert">{error}</p>}<button className="primary" disabled={loading} onClick={submit}>{loading?'REGISTERING…':'REGISTER'}</button><p className="footer-note">Your PIN is protected and is never shown to administrators.</p></section></main>}
