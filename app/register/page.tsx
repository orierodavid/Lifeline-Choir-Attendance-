'use client'
import {useState} from 'react'
import {LIFELINE_LOGO} from '../lifeline-logo'
const VOICE_PARTS=['Soprano','Alto','Tenor','Musician']
function LifelineLogo(){return <div className="member-logo" aria-label="Calvary Bible Church"><img src={LIFELINE_LOGO} alt="Calvary Bible Church logo"/></div>}
function validPin(p:string){if(!/^\d{4}$/.test(p)||new Set(p).size!==4)return false;const n=p.split('').map(Number);return !(n.every((v,i)=>i===0||v===n[i-1]+1)||n.every((v,i)=>i===0||v===n[i-1]-1))}
export default function Register(){const[first_name,setFirst]=useState(''),[last_name,setLast]=useState(''),[voice_part,setVoice]=useState(''),[pin,setPin]=useState(''),[confirm,setConfirm]=useState(''),[error,setError]=useState(''),[done,setDone]=useState(false),[loading,setLoading]=useState(false);async function submit(){setError('');if(!first_name.trim()||!last_name.trim()||!VOICE_PARTS.includes(voice_part))return setError('Complete your details to continue.');if(!validPin(pin))return setError('PIN must contain four different digits. Avoid simple sequences such as 1234.');if(pin!==confirm)return setError('PIN confirmation does not match.');setLoading(true);try{const r=await fetch('/api/register',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({first_name:first_name.trim(),last_name:last_name.trim(),voice_part,pin})});const d=await r.json();if(!r.ok)throw new Error(d.error||'Registration failed');setDone(true)}catch(e:any){setError(e.message||'Registration failed.')}finally{setLoading(false)}}return <main className="member-page"><header className="member-header"><div className="member-brand"><LifelineLogo/><div><b>CALVARY BIBLE CHURCH</b><span>Lifeline Choir</span></div></div><span className="member-status">MEMBER REGISTRATION</span></header><section className="member-main">{done?<div className="member-card success-card"><div className="success-icon">✓</div><span className="eyebrow">PROFILE CREATED</span><h1>Welcome to Lifeline.</h1><p>Your member profile is ready. Keep your four-digit PIN private.</p><a className="member-button" href="/">CONTINUE TO CHECK-IN</a></div>:<div className="member-card register-card"><div className="member-intro"><span className="eyebrow">LIFELINE CHOIR · NEW MEMBER</span><h1>Create your profile.</h1><p>Register once. Use the same details whenever you check in.</p></div><div className="section-label">YOUR DETAILS</div><div className="member-form two-col"><div className="field"><label>FIRST NAME</label><input value={first_name} onChange={e=>setFirst(e.target.value)} placeholder="First name" autoComplete="given-name"/></div><div className="field"><label>LAST NAME</label><input value={last_name} onChange={e=>setLast(e.target.value)} placeholder="Last name" autoComplete="family-name"/></div></div><div className="field"><label>CHOIR ROLE</label><select value={voice_part} onChange={e=>setVoice(e.target.value)}><option value="">Select your role</option>{VOICE_PARTS.map(v=><option key={v}>{v}</option>)}</select></div><div className="section-label pin-label">CREATE YOUR PIN</div><div className="pin-help">Use four different digits. Avoid simple sequences such as 1234.</div><div className="member-form two-col"><div className="field"><label>4-DIGIT PIN</label><input className="pin" inputMode="numeric" maxLength={4} value={pin} onChange={e=>setPin(e.target.value.replace(/\D/g,'').slice(0,4))} placeholder="••••"/></div><div className="field"><label>CONFIRM PIN</label><input className="pin" inputMode="numeric" maxLength={4} value={confirm} onChange={e=>setConfirm(e.target.value.replace(/\D/g,'').slice(0,4))} placeholder="••••"/></div></div>{error&&<p className="member-error">{error}</p>}<button className="member-button" disabled={loading} onClick={submit}>{loading?'CREATING PROFILE…':'CREATE PROFILE'}</button><div className="member-footer"><span>Already registered?</span><a href="/">Go to check-in</a></div></div>}</section></main>}
