'use client'
import {useState} from 'react'
const VOICE_PARTS=['Soprano','Alto','Tenor','Musician']
function validPin(p:string){if(!/^\d{4}$/.test(p)||new Set(p).size!==4)return false;const n=p.split('').map(Number);return !(n.every((v,i)=>i===0||v===n[i-1]+1)||n.every((v,i)=>i===0||v===n[i-1]-1))}
export default function Register(){const[first_name,setFirst]=useState(''),[last_name,setLast]=useState(''),[voice_part,setVoice]=useState(''),[pin,setPin]=useState(''),[confirm,setConfirm]=useState(''),[error,setError]=useState(''),[done,setDone]=useState(false),[loading,setLoading]=useState(false);async function submit(){setError('');if(!first_name.trim()||!last_name.trim()||!VOICE_PARTS.includes(voice_part))return setError('Complete your name and select your role.');if(!validPin(pin))return setError('Choose four different digits. Simple sequences such as 1234 are not allowed.');if(pin!==confirm)return setError('Your PINs do not match.');setLoading(true);try{const r=await fetch('/api/register',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({first_name:first_name.trim(),last_name:last_name.trim(),voice_part,pin})});const d=await r.json();if(!r.ok)throw new Error(d.error||'Registration failed');setDone(true)}catch(e:any){setError(e.message||'Registration failed.')}finally{setLoading(false)}}if(done)return <main className="public-shell"><section className="public-card success"><div className="eyebrow">CALVARY BIBLE CHURCH</div><div className="public-brand">Lifeline <span>Choir Attendance</span></div><div className="success-icon">✓</div><h1>Registration complete</h1><p>Your choir profile is ready. Keep your PIN private — administrators cannot see it.</p><a className="primary public-cta" href="/">Continue to check-in</a></section></main>;return <main className="public-shell"><section className="public-card"><div className="eyebrow">CALVARY BIBLE CHURCH</div><div className="public-brand">Lifeline <span>Choir Attendance</span></div><div className="public-intro"><span>MEMBER REGISTRATION</span><h1>Join the choir</h1><p>Register once. Your name, role and private PIN will be used for attendance.</p></div><div className="form-grid"><div className="field"><label className="label">First name</label><input className="input" value={first_name} onChange={e=>setFirst(e.target.value)} placeholder="e.g. David" autoComplete="given-name"/></div><div className="field"><label className="label">Last name</label><input className="input" value={last_name} onChange={e=>setLast(e.target.value)} placeholder="e.g. Cargoland" autoComplete="family-name"/></div></div><div className="field"><label className="label">Voice part / role</label><select className="input" value={voice_part} onChange={e=>setVoice(e.target.value)}><option value="">Choose a role</option>{VOICE_PARTS.map(v=><option key={v}>{v}</option>)}</select></div><div className="pin-grid"><div className="field"><label className="label">Create PIN</label><input className="input pin" inputMode="numeric" maxLength={4} value={pin} onChange={e=>setPin(e.target.value.replace(/\D/g,'').slice(0,4))} placeholder="••••"/></div><div className="field"><label className="label">Confirm PIN</label><input className="input pin" inputMode="numeric" maxLength={4} value={confirm} onChange={e=>setConfirm(e.target.value.replace(/\D/g,'').slice(0,4))} placeholder="••••"/></div></div><div className="security-note"><strong>Your PIN stays private</strong><span>It is never displayed to administrators.</span></div>{error&&<p className="form-error" role="alert">{error}</p>}<button className="primary" disabled={loading} onClick={submit}>{loading?'CREATING PROFILE…':'CREATE PROFILE'}</button><p className="footer-note">Already registered? <a className="link-text" href="/">Go to attendance check-in</a></p></section></main>}