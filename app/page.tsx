'use client'
import {useEffect,useState} from 'react'
export default function Home(){const[name,setName]=useState(''),[pin,setPin]=useState(''),[status,setStatus]=useState(''),[done,setDone]=useState(false),[loading,setLoading]=useState(false),[locationStatus,setLocationStatus]=useState('Location access is required to check in.'),[venue,setVenue]=useState<any>({church_name:'Loading attendance location…',church_radius_meters:300});useEffect(()=>{fetch('/api/location',{cache:'no-store'}).then(r=>r.json()).then(d=>{if(d.church_name)setVenue(d)}).catch(()=>setVenue({church_name:'Attendance location',church_radius_meters:300}))},[]);async function requestLocation(){if(!window.isSecureContext){setStatus('Location access requires a secure HTTPS connection.');return null}if(!navigator.geolocation){setStatus('This browser does not support location services.');return null}setLoading(true);setStatus('');setLocationStatus('Requesting location permission…');try{const p=await new Promise<GeolocationPosition>((resolve,reject)=>navigator.geolocation.getCurrentPosition(resolve,reject,{enableHighAccuracy:true,timeout:20000,maximumAge:0}));setLocationStatus(`Location ready · ±${Math.round(p.coords.accuracy)}m accuracy`);return p}catch(e:any){const c=e?.code;if(c===1){setStatus('Location permission was denied. Allow Location for this website in your browser settings, then try again.');setLocationStatus('Location permission denied.')}else if(c===2){setStatus('Your device could not determine its location. Turn on Location Services and try again.');setLocationStatus('Location unavailable.')}else if(c===3){setStatus('Location request timed out. Make sure Location Services are on and try again.');setLocationStatus('Location request timed out.')}else setStatus('Unable to access your location.');return null}finally{setLoading(false)}}async function checkIn(){setStatus('');const p=await requestLocation();if(!p)return;setLoading(true);try{const r=await fetch('/api/check-in',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({name,pin,lat:p.coords.latitude,lng:p.coords.longitude,accuracy:p.coords.accuracy})});const d=await r.json();if(!r.ok)throw new Error(d.error||'Check-in failed');setLocationStatus('Location verified.');setName(d.member?.name||name.trim());setDone(true)}catch(e:any){setStatus(e.message||'Unable to complete check-in.')}finally{setLoading(false)}}if(done)return <main className="shell"><section className="card success"><div className="logo-mark">✦</div><div className="brand">CALVARY BIBLE CHURCH</div><div className="system-name">Lifeline Choir Attendance System</div><div className="check">✓</div><h1 className="title">You&apos;re marked present</h1><p className="sub">{name} · Your attendance has been recorded.</p></section></main>;return <main className="shell"><section className="card"><div className="logo-mark">✦</div><div className="brand">CALVARY BIBLE CHURCH</div><div className="system-name">Lifeline Choir Attendance System</div><div className="rule"/><h1 className="title">Choir Check-in</h1><p className="sub">Enter your first name or last name and PIN. Your browser will then ask for permission to use your location.</p><div className="panel" style={{margin:'0 0 18px',padding:14}}><strong>📍 {venue.church_name}</strong><div className="muted">Attendance is valid within {venue.church_radius_meters||300}m of this location.</div></div><div className="field"><label className="label">First or Last Name</label><input className="input" value={name} onChange={e=>setName(e.target.value)} placeholder="Enter your name" disabled={loading}/></div><div className="field"><label className="label">4-Digit PIN</label><input className="input pin" value={pin} onChange={e=>setPin(e.target.value.replace(/\D/g,'').slice(0,4))} inputMode="numeric" maxLength={4} placeholder="••••" disabled={loading}/></div><div className="location"><span className="dot"/> {locationStatus}</div>{status&&<p className="muted" role="alert" style={{marginBottom:16}}>{status}</p>}<button className="primary" disabled={loading||!name.trim()||pin.length!==4} onClick={checkIn}>{loading?'REQUESTING LOCATION…':'CHECK IN'}</button><p className="footer-note">Location is checked against the attendance location selected by the administrator.</p></section></main>}
