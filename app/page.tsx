'use client'
import {useEffect,useState} from 'react'
export default function Home(){const[name,setName]=useState(''),[pin,setPin]=useState(''),[status,setStatus]=useState(''),[done,setDone]=useState(false),[loading,setLoading]=useState(false),[locationStatus,setLocationStatus]=useState('Location access is required'),[venue,setVenue]=useState<any>(null);useEffect(()=>{fetch('/api/location',{cache:'no-store'}).then(r=>r.json()).then(d=>{if(d.church_name)setVenue(d);else setStatus(d.error||'Unable to load attendance location.')}).catch(()=>setStatus('Unable to load attendance location.'))},[]);async function requestLocation(){if(!window.isSecureContext){setStatus('Location access requires a secure HTTPS connection.');return null}if(!navigator.geolocation){setStatus('This browser does not support location services.');return null}setLoading(true);setStatus('');setLocationStatus('Requesting location permission…');try{const p=await new Promise<GeolocationPosition>((resolve,reject)=>navigator.geolocation.getCurrentPosition(resolve,reject,{enableHighAccuracy:true,timeout:20000,maximumAge:0}));setLocationStatus(`Location ready · ±${Math.round(p.coords.accuracy)}m accuracy`);return p}catch(e:any){const c=e?.code;setLocationStatus(c===1?'Location permission denied':c===2?'Location unavailable':c===3?'Location request timed out':'Unable to access location');setStatus(c===1?'Allow Location for this website in your browser settings, then try again.':c===2?'Turn on Location Services and try again.':c===3?'Make sure Location Services are on and try again.':'Unable to access your location.');return null}finally{setLoading(false)}}async function checkIn(){setStatus('');const p=await requestLocation();if(!p)return;setLoading(true);try{const r=await fetch('/api/check-in',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({name,pin,lat:p.coords.latitude,lng:p.coords.longitude,accuracy:p.coords.accuracy})});const d=await r.json();if(!r.ok)throw new Error(d.error||'Check-in failed');setName(d.member?.name||name.trim());setDone(true)}catch(e:any){setStatus(e.message||'Unable to complete check-in.')}finally{setLoading(false)}}if(done)return <main className="public-shell"><section className="public-card success"><div className="eyebrow">CALVARY BIBLE CHURCH</div><div className="public-brand">Lifeline <span>Choir Attendance</span></div><div className="success-icon">✓</div><h1>You&apos;re marked present</h1><p>{name} · Your attendance has been recorded successfully.</p></section></main>;return <main className="public-shell"><section className="public-card checkin-card"><div className="eyebrow">CALVARY BIBLE CHURCH</div><div className="public-brand">Lifeline <span>Choir Attendance</span></div><div className="public-intro"><span>ATTENDANCE CHECK-IN</span><h1>Mark your presence</h1><p>Enter your details and verify your location. Your PIN remains private.</p></div>{venue&&<div className="venue-card"><div className="venue-icon">⌖</div><div><small>ATTENDANCE LOCATION</small><strong>{venue.church_name}</strong><span>Within {venue.church_radius_meters||300}m · Location verified by GPS</span></div></div>}<div className="field"><label className="label">First or last name</label><input className="input" value={name} onChange={e=>setName(e.target.value)} placeholder="Enter your name" autoComplete="name" disabled={loading}/></div><div className="field"><label className="label">4-digit PIN</label><input className="input pin" value={pin} onChange={e=>setPin(e.target.value.replace(/\D/g,'').slice(0,4))} inputMode="numeric" maxLength={4} placeholder="••••" disabled={loading}/></div><div className="location"><span className="dot"/> {locationStatus}</div>{status&&<p className="form-error" role="alert">{status}</p>}<button className="primary" disabled={loading||!name.trim()||pin.length!==4} onClick={checkIn}>{loading?'VERIFYING LOCATION…':'CHECK IN'}</button><p className="footer-note">Your location is used only to verify that you are within the selected attendance area.</p><p className="footer-note"><a className="link-text" href="/register">Not registered? Register here</a></p></section></main>}